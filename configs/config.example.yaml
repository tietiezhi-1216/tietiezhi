# 沙箱配置
sandbox:
  enabled: true
  image: "alpine:latest"
  network_mode: "none"         # none=禁止网络，bridge=允许网络
  memory_limit: "128m"
  cpu_limit: 0.5
  work_dir: "/workspace"
  volumes:
    - host_path: "./data/workspace"
      container_path: "/workspace"
      read_only: true
