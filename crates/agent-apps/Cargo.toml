[package]
name = "tietiezhi-agent-apps"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
tietiezhi-agent-protocol = { path = "../agent-protocol" }
