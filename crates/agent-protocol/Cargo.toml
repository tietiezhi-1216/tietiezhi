[package]
name = "tietiezhi-agent-protocol"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
doctest = false

[dependencies]
schemars = "0.8.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
typify = "0.7.0"
