[package]
name = "tietiezhi-agent-skills"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
doctest = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
tietiezhi-agent-config = { path = "../agent-config" }
tietiezhi-agent-tools = { path = "../agent-tools" }
walkdir = "2"

[dev-dependencies]
tempfile = "3"
